Write a function `string_length_lists(lst)` that takes a list of strings and returns a list of list. Each inner list should contain a string and its corresponding length.


```python
string_length_tuples(["apple", "banana", "cherry"]) 
# returns [['apple', 5], ['banana', 6], ['cherry', 6]]

string_length_tuples(["hello", "world"])
# returns [['hello', 5], ['world', 5]]

string_length_tuples(["a", "bb", "ccc", "dddd"])
# returns [['a', 1], ['bb', 2], ['ccc', 3], ['dddd', 4]]
```
