Write a function `make_abba(a: str, b: str) -> str` that returns a string in the pattern `a + b + b + a`.
Examples:
- `"Hi", "Bye"` → `"HiByeByeHi"`
- `"Yo", "Alice"` → `"YoAliceAliceYo"`
- `"What", "Up"` → `"WhatUpUpWhat"`