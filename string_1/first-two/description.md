Write a function `first_two(s: str) -> str` that returns the first two characters of a string. If the string is shorter than two characters, return it as-is.
Examples:
- `"Hello"` → `"He"`
- `"X"` → `"X"`
- `""` → `""`