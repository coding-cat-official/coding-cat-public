Write a function `is_half_digit(s: str) -> bool` that returns `True` if **all** characters in the first half of `s` are digits.  
Assume `len(s)` is even.  
Examples: `"123abc"` → `True`; `"12a3bc"` → `False`.