Write a function `any_two_equal(a: int, b: int, c: int) -> bool` that takes three integer inputs: `a`, `b`, and `c`. The function should return `True` if any two of the three input numbers are the same, and `False` otherwise. 

For example:
- `any_two_equal(3, 3, 4) → True` because `a` and `b` are the same.
